package com.systematic.kombit.administration;

import javax.ejb.Local;

/**
 * This interface defines the local interface for the administering the connection and service agreements
 */
@Local
public interface AdministrationLocal extends Administration {
}
